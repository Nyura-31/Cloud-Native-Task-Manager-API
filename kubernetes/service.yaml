# Provides a stable in-cluster endpoint for the Task Manager API pods.
apiVersion: v1
kind: Service
metadata:
  name: task-manager-api
  namespace: task-manager
spec:
  type: ClusterIP
  selector:
    app: task-manager-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
